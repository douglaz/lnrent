/target
**/target
/data
*.sqlite
*.sqlite-*

# local scratch dirs used while reproducing bootstrap/data-dir bugs (cfg.toml/err/out, sqlite, etc.)
.tmp-test/

# bv (beads viewer) local config and caches
.bv/

# beads local db (issues.jsonl is the tracked source)
.beads/*.db
.beads/*.db-wal
.beads/*.db-shm
