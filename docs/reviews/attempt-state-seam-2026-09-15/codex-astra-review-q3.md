FAIL — the proposed three-predicate design needs accounting and observation semantics clarified before adoption.

[P1] daemon/src/sweep.rs:133 — **The proposal introduces double subtraction.** Today, a started PENDING refund contributes gross to `reserved`, and nothing to `paid_out`; the receipt map deduplicates reservations (daemon/src/sweep.rs:136,159). Under the proposal, both Owed and Committed hold, so the formula subtracts gross twice (daemon/src/sweep.rs:62). For 200 sats received and one 100-sat refund, surplus falls from 100 to 0. Preserve exclusive accounting contributions per receipt.

[P2] daemon/src/ledger.rs:76 — **Committed must describe current evidence, not “ever started.”** Backend Failed overrides historical started evidence. For example, lnv2 records terminal failure in its backend row (daemon/src/lnv2_backend.rs:853); the refunder increments attempts, retaining PENDING below the retry cap and recording FAILED at exhaustion (daemon/src/refund.rs:483,1124,1138,1144). Commitment therefore legitimately reverses. Expected holdings re-evaluates it for readiness, holdings-floor checks, and reconcile (daemon/src/supervisor.rs:1760,1256; daemon/src/ipc.rs:851). Readiness independently probes again (daemon/src/supervisor.rs:1866): define a shared observation boundary so opposite sides of a transition cannot produce inconsistent operands.

[P2] daemon/src/supervisor.rs:1808 — **“Required liquidity” contradicts the promise to exclude failed-parked refunds.** Today FAILED contributes to gross liability and parked count, but skips required liquidity (daemon/src/supervisor.rs:1805). A fenced FAILED refund remains FAILED after clearance; clearance only removes the fence and directs the operator to retry (daemon/src/ipc.rs:1280,1309). If backend evidence then establishes no commitment, Owed ∧ ¬Committed becomes true despite continued parking. Choose explicitly whether the metric covers all unfunded obligations or only automatic payments. Keep debt visible before clearance; its liquidity classification may change without creating new debt.

[P2] daemon/src/sweep.rs:149 — **Attempt predicates cannot express every receipt reservation.** Surplus reserves at-risk receipts independently of refund attempts. Readiness likewise includes paid-undelivered orders without attempts (daemon/src/store.rs:1352,1365,1375) and charges them to required liquidity (daemon/src/supervisor.rs:1827). Preserve a receipt-level AtRisk predicate; `reserved = Owed` is incomplete.

[P2] daemon/src/sweep.rs:858 — **Slot occupancy is a separate execution predicate.** A fenced PENDING sweep blocks another invoice; SENT and fenced FAILED sweeps do not occupy that slot, although all are Committed under the proposal (daemon/src/ledger.rs:203). Their identical `(Committed=true, Owed=false, Required=false)` values cannot express this distinction. Preserve `OccupiesSweepSlot`; same-invoice fence refusal is another explicit guard (daemon/src/sweep.rs:838).

[P2] daemon/src/phoenixd_backend.rs:2218 — **The “live backend call” premise needs correction.** Phoenixd’s status/started methods read SQLite; lnv2’s do likewise (daemon/src/lnv2_backend.rs:1408). These can belong to observed Attempt state, consistent with CONTEXT.md:190, but exceed the attempt row alone. Moreover, PREPARED maps to Pending before the refund’s pay call (daemon/src/lnv2_backend.rs:484; daemon/src/refund.rs:449,468). Define Committed as a conservative accounting exclusion, not proof funds physically left.

The question is timely, but first establish what each reader measures and how one receipt contributes exactly once. That does not reopen the accepted separation of Attempt state from money.

Three are useful names, not three sufficient independent predicates. Required liquidity is derived from the other two, with an unresolved parking policy. Receipt risk and execution eligibility remain separate concerns. A non-monotonic Committed predicate is valid when evaluated from current, consistently observed evidence; a permanent “started” flag is not an equivalent substitute.