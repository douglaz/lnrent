FAIL as specified — the Q4 design against `7927bfb` and working-tree `CONTEXT.md` needs an evidence/policy boundary and shared report observations.

[P2] [supervisor.rs:1760](/home/master/projects/lnrent/daemon/src/supervisor.rs:1760) — **Existing inconsistency survives one reader.** Expected holdings probes refunds first ([ledger.rs:76](/home/master/projects/lnrent/daemon/src/ledger.rs:76)); readiness probes again ([supervisor.rs:1866](/home/master/projects/lnrent/daemon/src/supervisor.rs:1866)). With 1,000 sats received and a 600-sat refund changing Pending→Failed between probes, the report can compare 400 expected against 600 required; one consistent observation yields coverage. Share observations, including row generation and fence.

[P2] [refund.rs:475](/home/master/projects/lnrent/daemon/src/refund.rs:475) — **Resolve the prior consumer question first.** Recovery needs Succeeded/Failed/Pending/Unknown and caller-specific error handling, not merely Committed. Failure controls retry accounting; success records SENT. [sweep.rs:513](/home/master/projects/lnrent/daemon/src/sweep.rs:513) likewise selects adoption, re-await, failure, or re-gating. A shared evidence reader is suitable; a collapsed money predicate is insufficient.

[P2] [lnv2_backend.rs:484](/home/master/projects/lnrent/daemon/src/lnv2_backend.rs:484) — **PREPARED over-approximates actual funding.** Refund authorization precedes payment ([refund.rs:449](/home/master/projects/lnrent/daemon/src/refund.rs:449)). Conservative commitment can include this ambiguity, but driver eligibility must permit same-key recovery: the backend checks operation existence ([lnv2_backend.rs:802](/home/master/projects/lnrent/daemon/src/lnv2_backend.rs:802)) and enforces the cap before new funding ([lnv2_backend.rs:931](/home/master/projects/lnrent/daemon/src/lnv2_backend.rs:931)). Never interpret Committed as “skip paying.”

[P2] [lnv2_backend.rs:1197](/home/master/projects/lnrent/daemon/src/lnv2_backend.rs:1197) — **The sweep construction argument is too strong.** `prepare_pay` can persist FAILED on collision while [sweep.rs:874](/home/master/projects/lnrent/daemon/src/sweep.rs:874) inserts PENDING. Therefore PENDING does not universally prove started evidence. Preserve the conservative SQL reservation until sweep terminalization; do not justify it through an exact started invariant.

[P3] [order_intake.rs:1440](/home/master/projects/lnrent/daemon/src/order_intake.rs:1440) — **Correct the caller census.** The cited key-method occurrences here, in [reconcile.rs:1628](/home/master/projects/lnrent/daemon/src/reconcile.rs:1628), and in [ipc.rs:1713](/home/master/projects/lnrent/daemon/src/ipc.rs:1713) are test code, not production consumer exceptions. Production IPC money/reconcile already delegates through readiness/ledger ([ipc.rs:808](/home/master/projects/lnrent/daemon/src/ipc.rs:808), [ipc.rs:851](/home/master/projects/lnrent/daemon/src/ipc.rs:851)).

**Suggestions**

- Use an owned `AttemptState` containing lifecycle, fence, kind-derived key, and full payment evidence. Build it through an async function taking an owned row and `&dyn PaymentBackend`. Keep predicates pure; preserve lookup errors. Neither an awaited `&Connection` nor a Store-owned backend is needed.
- Load report inputs together; observe each attempt once and pass that report-scoped collection to holdings and liquidity calculations. This removes duplicate observations, but does **not** constitute an atomic database snapshot across awaits. Include the missing fence from [store.rs:1284](/home/master/projects/lnrent/daemon/src/store.rs:1284). Independent holdings-floor checks may obtain fresh observations ([supervisor.rs:1256](/home/master/projects/lnrent/daemon/src/supervisor.rs:1256)).
- Keep audits as separate transactional commands. Positive adoption must record SENT with required bookkeeping and fence clearance. **QUESTION:** the future uxbd/gjwy write contract is unverifiable here. Existing clear-fence only clears/journals ([ipc.rs:1280](/home/master/projects/lnrent/daemon/src/ipc.rs:1280)); refund success currently accepts only PENDING ([refund.rs:992](/home/master/projects/lnrent/daemon/src/refund.rs:992)).
- Extend [clippy.toml:1](/home/master/projects/lnrent/clippy.toml:1) with both fully qualified key methods; narrowly allow the evidence reader and test delegates. Runnable check: `nix develop . --command cargo clippy --workspace --all-targets -- -D warnings`. Demonstrate rejection with a temporary direct production call. This guards method bypasses, not raw SQL or new lint allowances. Not run in this read-only review.

**Reasoning**

The surplus can remain synchronous: non-SENT refunds reserve once ([sweep.rs:133](/home/master/projects/lnrent/daemon/src/sweep.rs:133)), and pending/sent/fenced sweeps retain their caps ([sweep.rs:169](/home/master/projects/lnrent/daemon/src/sweep.rs:169)).

The sweep gate has no new TOCTOU **provided its transactional checks remain**: fence, existing status, Busy, surplus, intent, and backend persistence execute together ([sweep.rs:807](/home/master/projects/lnrent/daemon/src/sweep.rs:807)). An earlier async observation cannot replace those checks. Recovery already separates its surplus read from authorization ([sweep.rs:575](/home/master/projects/lnrent/daemon/src/sweep.rs:575)); do not claim the current transactional guarantee covers that path.

Keep `outbound_status_by_ref` outside report observations. It supplies fresh decision-time evidence for adoption or terminalization ([sweep.rs:678](/home/master/projects/lnrent/daemon/src/sweep.rs:678)). Share evidence interpretation, while retaining accounting, recovery, and transactional authorization as distinct consumers.